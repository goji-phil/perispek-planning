---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/globals.css
  - src/lib/utils/cn.ts
  - next.config.ts
  - tsconfig.json
autonomous: true
requirements:
  - NAV-03

must_haves:
  truths:
    - "The Next.js app boots without errors at localhost:3000"
    - "Tailwind design tokens are defined in globals.css @theme and produce Tailwind utility classes"
    - "The cn() utility merges Tailwind classes without conflicts"
  artifacts:
    - path: "src/app/globals.css"
      provides: "All Figma design tokens via @theme directive"
      contains: "@theme"
    - path: "src/lib/utils/cn.ts"
      provides: "cn() class merge utility"
      exports: ["cn"]
    - path: "package.json"
      provides: "All required dependencies installed"
      contains: "@phosphor-icons/react"
  key_links:
    - from: "src/app/globals.css"
      to: "Tailwind CSS v4 utility classes"
      via: "@theme directive"
      pattern: "@theme\\s*\\{"
    - from: "src/lib/utils/cn.ts"
      to: "clsx + tailwind-merge"
      via: "import statements"
      pattern: "twMerge.*clsx"
---

<objective>
Bootstrap the Next.js 15 application with all dependencies installed and the complete Figma-derived design token system defined in Tailwind v4 @theme format.

Purpose: Every subsequent plan depends on the token system existing — colors, typography, spacing, and sidebar dimensions must be defined before any component can use semantic Tailwind classes.
Output: A running Next.js app with all dependencies, cn() utility, and complete @theme token set in globals.css.
</objective>

<execution_context>
@/Users/philipdavies/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipdavies/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js project and install dependencies</name>
  <files>package.json, next.config.ts, tsconfig.json, .eslintrc.json</files>
  <action>
    Run from /Users/philipdavies (the working directory where the project should be created):

    ```bash
    npx create-next-app@latest perispek \
      --typescript \
      --eslint \
      --app \
      --src-dir \
      --turbopack \
      --no-tailwind \
      --import-alias "@/*"
    cd perispek
    npm install tailwindcss @tailwindcss/postcss postcss
    npm install @phosphor-icons/react zustand clsx tailwind-merge
    ```

    Then configure PostCSS for Tailwind v4. Create `postcss.config.mjs`:
    ```js
    const config = {
      plugins: {
        '@tailwindcss/postcss': {},
      },
    };
    export default config;
    ```

    IMPORTANT: Do NOT create tailwind.config.ts — Tailwind v4 uses CSS-first configuration via @theme in globals.css. Creating a config file would introduce v3 patterns into a v4 project.

    Verify tsconfig.json has `"paths": { "@/*": ["./src/*"] }` for the import alias. This is set by create-next-app with --import-alias "@/*".
  </action>
  <verify>
    From /Users/philipdavies/perispek:
    - `npm run build` completes without errors
    - `ls node_modules/@phosphor-icons` shows the package installed
    - `ls node_modules/zustand` shows the package installed
    - `cat package.json | grep -E "(phosphor|zustand|clsx|tailwind-merge)"` shows all 4 dependencies
  </verify>
  <done>
    Next.js 15 project exists at /Users/philipdavies/perispek with all dependencies installed. Build succeeds. No tailwind.config.ts file exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define Figma design tokens and cn() utility</name>
  <files>src/app/globals.css, src/lib/utils/cn.ts</files>
  <action>
    **FIRST: Open the Figma file to extract exact design token values.**
    Figma URL: https://www.figma.com/design/ZZRoLzpoTNy4kpT1NPLrOS/IDEX-x-Goji----Hi-Fi-for-Perispek?node-id=0-1

    Extract from Figma:
    - Brand/primary color (main interactive blue)
    - Background color (page/app background)
    - Surface colors (sidebar, cards, panels — typically 2-3 levels)
    - Foreground/text colors (primary text, muted/secondary text)
    - Border color
    - Typography: font family, text sizes used in nav/labels
    - Border radius values (small buttons, cards, large modals)

    If Figma access fails (requires login, no credentials available), use the research-documented approximations and add a TODO comment to refine. The approximations from RESEARCH.md are visually correct dark-theme dashboard colors.

    Write `src/app/globals.css`:
    ```css
    @import "tailwindcss";

    @theme {
      /* === Colors — extracted from Figma === */
      /* Primary brand (interactive elements, active states) */
      --color-primary: oklch(0.58 0.19 240);
      --color-primary-hover: oklch(0.52 0.19 240);

      /* Backgrounds */
      --color-background: #080b12;       /* App page background */
      --color-surface: #0f1117;          /* Sidebar, panel backgrounds */
      --color-surface-hover: #1a1d26;    /* Hover state for nav items */
      --color-surface-active: #1e2235;   /* Active/selected nav item bg */

      /* Text */
      --color-foreground: #e8eaf0;
      --color-foreground-muted: #8b90a0;

      /* Border */
      --color-border: #2a2d3a;

      /* Risk band colors */
      --color-risk-low: oklch(0.72 0.18 145);      /* Green */
      --color-risk-medium: oklch(0.78 0.18 75);    /* Amber */
      --color-risk-high: oklch(0.68 0.22 35);      /* Orange */
      --color-risk-critical: oklch(0.60 0.25 25);  /* Red */

      /* Status colors (IoT sensor states) */
      --color-status-normal: oklch(0.72 0.18 145);
      --color-status-warning: oklch(0.78 0.18 75);
      --color-status-overflow: oklch(0.60 0.25 25);

      /* === Typography === */
      --font-sans: 'Inter', system-ui, -apple-system, sans-serif;

      /* === Spacing / Border Radius === */
      --radius-sm: 4px;
      --radius-md: 8px;
      --radius-lg: 12px;
      --radius-xl: 16px;

      /* === Layout === */
      --sidebar-width: 240px;
      --sidebar-collapsed-width: 64px;
      --topbar-height: 56px;
    }

    /* Base styles */
    * {
      box-sizing: border-box;
    }

    body {
      font-family: var(--font-sans);
      background-color: var(--color-background);
      color: var(--color-foreground);
      margin: 0;
    }

    /* Page transition animation */
    @layer components {
      .page-fade-in {
        animation: pageFadeIn 150ms ease-out;
      }

      @keyframes pageFadeIn {
        from {
          opacity: 0;
          transform: translateY(4px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    }
    ```

    Write `src/lib/utils/cn.ts`:
    ```ts
    import { clsx, type ClassValue } from 'clsx'
    import { twMerge } from 'tailwind-merge'

    /**
     * Combines clsx conditional class logic with tailwind-merge conflict resolution.
     * Use for every component that conditionally applies Tailwind classes.
     */
    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs))
    }
    ```

    Create the utils directory if it doesn't exist: `src/lib/utils/`.
  </action>
  <verify>
    From /Users/philipdavies/perispek:
    - `npm run build` succeeds with no errors
    - `grep "@theme" src/app/globals.css` returns the theme block
    - `grep "sidebar-width" src/app/globals.css` returns the CSS variable definition
    - `cat src/lib/utils/cn.ts` shows the cn() function with both clsx and twMerge imports
    - `grep -r "tailwind.config" .` returns nothing (no config file created)
  </verify>
  <done>
    globals.css contains @import "tailwindcss" + @theme block with all color, typography, spacing, and layout tokens. src/lib/utils/cn.ts exports cn(). Build passes.
  </done>
</task>

</tasks>

<verification>
Run from /Users/philipdavies/perispek:
1. `npm run build` — must succeed with 0 errors
2. `npm run dev` — app starts at http://localhost:3000
3. `cat src/app/globals.css | grep "@theme"` — token block present
4. `ls src/lib/utils/cn.ts` — file exists
5. Confirm no `tailwind.config.ts` or `tailwind.config.js` file exists in project root
</verification>

<success_criteria>
- Next.js 15 project at /Users/philipdavies/perispek with TypeScript, ESLint, App Router, src/ directory
- Dependencies installed: @phosphor-icons/react, zustand, clsx, tailwind-merge, tailwindcss, @tailwindcss/postcss
- globals.css uses @import "tailwindcss" (Tailwind v4 syntax — NOT @tailwind base/components/utilities)
- @theme block defines all design tokens: colors (primary, background, surface, foreground, border, risk bands), typography, border radius, sidebar dimensions
- cn() utility in src/lib/utils/cn.ts combining clsx + tailwind-merge
- No tailwind.config.ts created (v4 uses CSS-first tokens)
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template.
</output>
