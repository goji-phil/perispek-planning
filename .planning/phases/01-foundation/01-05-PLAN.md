---
phase: 01-foundation
plan: "05"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-04"
files_modified: []
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07

must_haves:
  truths:
    - "App opens at localhost:3000 and shows the global layout matching Figma chrome"
    - "All four nav sections are reachable and active state is visually clear"
    - "Sidebar collapses to icon-only and persists across refresh"
    - "TypeScript compiles clean — all 75 assets, 30 work orders, and coverage data are type-valid"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout"
      contains: "Sidebar"
    - path: "src/lib/data/assets.ts"
      provides: "75 seeded assets"
      exports: ["ASSETS"]
    - path: "src/lib/data/work-orders.ts"
      provides: "~30 work orders"
      exports: ["WORK_ORDERS"]
    - path: "src/lib/data/inspections.ts"
      provides: "Coverage data"
      exports: ["INSPECTION_COVERAGE"]
  key_links:
    - from: "localhost:3000"
      to: "/dashboard"
      via: "redirect from root page"
      pattern: "redirect"
    - from: "all data modules"
      to: "src/types/index.ts"
      via: "TypeScript type imports"
      pattern: "import.*from.*@/types"
---

<objective>
Visual and functional verification of the complete Phase 1 foundation: app navigation shell working, all data modules seeded and type-safe, ready for Phase 2 to build upon.

Purpose: Confirms everything built in Plans 01-01 through 01-04 works together as an integrated foundation before any subsequent phase begins.
Output: Human sign-off that navigation works correctly and data is accessible.
</objective>

<execution_context>
@/Users/philipdavies/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipdavies/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build validation and data spot-check</name>
  <files></files>
  <action>
    Run all automated checks to confirm the foundation is solid before requesting human review.

    From /Users/philipdavies/perispek:

    1. **TypeScript validation:**
       ```bash
       npx tsc --noEmit
       ```
       Must complete with 0 errors.

    2. **Production build:**
       ```bash
       npm run build
       ```
       Must complete successfully. Note any warnings but do not fail for warnings.

    3. **Data integrity spot-checks** — read the source files directly to verify counts and distributions:
       - Count asset objects in src/lib/data/assets.ts (should be 75)
       - Count IoT-enabled assets (iot.enabled: true — should be ~35)
       - Count overflow assets (currentDRatio > 1.0 — should be ~2)
       - Count work orders in src/lib/data/work-orders.ts (should be ~30)
       - Confirm inspection coverage has 12 weekly + 12 monthly + 4 quarterly periods

    4. **Start dev server:**
       ```bash
       npm run dev
       ```
       Confirm it starts at localhost:3000 with no startup errors.

    Report the results of each check in the summary.
  </action>
  <verify>
    - `npx tsc --noEmit` exits with code 0
    - `npm run build` completes with "Compiled successfully" or equivalent
    - Asset count: 75, IoT enabled: ~35, Overflow: ~2
    - Work order count: ~30, Open: ~10
    - Weekly periods: 12, Quarterly: 4
    - Dev server starts at localhost:3000
  </verify>
  <done>
    All automated checks pass. TypeScript compiles clean. Build succeeds. Data module spot-check confirms correct counts and distributions. Dev server running.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of nav shell and data layer</name>
  <action>Present verification instructions to the user and await approval.</action>
  <what-built>
    Complete Phase 1 foundation:
    - Next.js 15 app with Tailwind v4 design tokens (Plan 01-01)
    - Collapsible sidebar, active nav, four section stubs (Plan 01-02)
    - 75 seeded assets with KC GIS coords, NASSCO data, IoT sensors (Plan 01-03)
    - ~30 work orders + inspection coverage data with consent decree story (Plan 01-04)
  </what-built>
  <how-to-verify>
    With `npm run dev` running at localhost:3000:

    **1. Layout and chrome (NAV-03)**
    - Open http://localhost:3000 — should redirect to /dashboard
    - Confirm: dark background, sidebar on left, topbar at top, content area fills remaining space
    - Confirm: sidebar shows logo mark + "Perispek" app name + four nav items

    **2. Navigation (NAV-01, NAV-02)**
    - Click each nav item: Dashboard, Map, System Insights, Work Orders
    - Confirm: URL changes to /dashboard, /map, /insights, /work-orders respectively
    - Confirm: Active nav item has visually distinct background color and filled icon
    - Confirm: Inactive nav items are muted/gray by comparison

    **3. Sidebar collapse (NAV-01)**
    - Click the collapse toggle (arrow button at sidebar bottom)
    - Confirm: Sidebar smoothly animates to narrow icon-only state (~64px)
    - Confirm: Nav labels disappear; icons remain centered
    - Refresh the page — confirm: sidebar stays collapsed (localStorage persisted)
    - Click toggle again — confirm: sidebar re-expands smoothly

    **4. Data type safety**
    In your terminal, run:
    ```bash
    cd /Users/philipdavies/perispek && npx tsc --noEmit && echo "TYPE CHECK PASSED"
    ```
    Confirm "TYPE CHECK PASSED" appears.

    **Report any visual issues** (colors off from Figma, layout spacing wrong, animation janky) or navigation issues (wrong routes, broken active states).
  </how-to-verify>
  <verify>Human confirms all four points above pass visual inspection.</verify>
  <done>User types "approved" or confirms all nav and data layer checks pass.</done>
  <resume-signal>Type "approved" if everything looks good, or describe any issues to fix before proceeding to Phase 2</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. TypeScript compiles with 0 errors
2. Production build succeeds
3. Human confirmed nav shell works: layout visible, all four sections reachable, active states correct
4. Human confirmed sidebar collapse/expand works and persists across refresh
5. Data spot-check confirms ~75 assets, ~35 IoT-enabled, ~2 overflow, ~30 work orders, 12 weekly + 4 quarterly coverage periods
</verification>

<success_criteria>
- App opens at localhost:3000 → redirects to /dashboard
- Global layout: dark theme, sidebar left, topbar top, content fills rest
- All four nav items navigate correctly with visible active state
- Sidebar collapses to icon-only via smooth CSS transition; state persists in localStorage
- TypeScript compiles clean: 0 errors across all source files
- All data modules accessible: 75 assets, ~30 work orders, inspection coverage with consent decree story
- Phase 2 can begin immediately: data layer is complete, navigation shell is working
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` following the summary template.
</output>
