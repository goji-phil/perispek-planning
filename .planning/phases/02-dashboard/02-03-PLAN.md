---
phase: 02-dashboard
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/app/dashboard/page.tsx
  - src/components/dashboard/MapPlaceholder.tsx
  - src/components/dashboard/RightColumn.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "User sees a no-scroll dashboard that fills the viewport — top bar (80px) + functions bar (80px) + metrics row (170px) + below-metrics area that grows to fill remaining height"
    - "User sees 4 metric cards in a row: Risk of overflow, Avg. risk score, Work orders, Inspections this month — all populated from computed seed data"
    - "User sees the map placeholder section on the left (~65% width) with Risk Score section title and static placeholder card"
    - "User sees the right column with 3 sections: Blockage Predictions (2×2 grid), Condition Changes (2×2 grid), Today's Rainfall (3-cell row)"
    - "All metric values in the right column are populated from computeDashboardMetrics()"
    - "Rainfall values are static: 0.51 total, 0.05 intensity avg, 0.17 year avg"
    - "No TypeScript errors and no build errors"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Full dashboard layout as Server Component"
      contains: "computeDashboardMetrics"
      exports:
        - "default DashboardPage"
    - path: "src/components/dashboard/MapPlaceholder.tsx"
      provides: "Static map section with section title and placeholder card"
      exports:
        - "MapPlaceholder"
    - path: "src/components/dashboard/RightColumn.tsx"
      provides: "Right column with all 3 stats sections"
      exports:
        - "RightColumn"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/lib/data/dashboard.ts"
      via: "computeDashboardMetrics() call"
      pattern: "computeDashboardMetrics"
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/dashboard/MetricLargeCard.tsx"
      via: "component usage"
      pattern: "MetricLargeCard"
    - from: "src/components/dashboard/RightColumn.tsx"
      to: "src/components/dashboard/MetricStateCard.tsx"
      via: "component usage"
      pattern: "MetricStateCard"
    - from: "src/components/dashboard/RightColumn.tsx"
      to: "src/components/dashboard/SectionTitle.tsx"
      via: "component usage"
      pattern: "SectionTitle"
---

<objective>
Assemble the complete dashboard page using the components and metrics from Plans 01 and 02, then verify the full layout renders correctly in the browser.

Purpose: Replaces the stub page.tsx with a full, data-driven layout matching the Figma spec. This is the deliverable for Phase 2.
Output: A working /dashboard route showing the complete system overview with live seed data.
</objective>

<execution_context>
@/Users/philipdavies/.claude/get-shit-done/workflows/execute-plan.md
@/Users/philipdavies/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/philipdavies/.planning/PROJECT.md
@/Users/philipdavies/.planning/ROADMAP.md
@/Users/philipdavies/.planning/STATE.md
@/Users/philipdavies/.planning/phases/02-dashboard/02-01-SUMMARY.md
@/Users/philipdavies/.planning/phases/02-dashboard/02-02-SUMMARY.md
@/Users/philipdavies/perispek/src/app/globals.css
@/Users/philipdavies/perispek/src/lib/data/dashboard.ts
@/Users/philipdavies/perispek/src/components/dashboard/MetricLargeCard.tsx
@/Users/philipdavies/perispek/src/components/dashboard/MetricStateCard.tsx
@/Users/philipdavies/perispek/src/components/dashboard/SectionTitle.tsx
@/Users/philipdavies/perispek/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MapPlaceholder and RightColumn components</name>
  <files>
    src/components/dashboard/MapPlaceholder.tsx
    src/components/dashboard/RightColumn.tsx
  </files>
  <action>
    Create two container components that will be assembled in the page.

    **MapPlaceholder.tsx** — Left panel (~65% of below-metrics area). Contains the "Risk Score" section title and a static dark card filling remaining height.

    ```tsx
    // src/components/dashboard/MapPlaceholder.tsx
    // Server Component — no 'use client'
    import {
      Diamond,
      MagnifyingGlass,
      ArrowsOut,
      Minus,
      Plus,
      NavigationArrow,
    } from '@phosphor-icons/react/ssr'
    import { SectionTitle } from './SectionTitle'
    import { cn } from '@/lib/utils/cn'

    interface MapPlaceholderProps {
      className?: string
    }

    export function MapPlaceholder({ className }: MapPlaceholderProps) {
      return (
        <div className={cn('flex flex-col gap-[10px] min-h-0', className)}>
          {/* Section title */}
          <SectionTitle
            icon={<Diamond size={24} className="text-foreground-muted" />}
            title="Risk Score"
            linkText="View full map"
          />
          {/* Map placeholder card — fills remaining height */}
          <div className="relative flex flex-1 min-h-0 rounded-[12px] bg-[#1c1c1e] border border-[#3f3f46] overflow-hidden">
            {/* Left toolbar chrome */}
            <div className="absolute left-3 top-1/2 -translate-y-1/2 flex flex-col gap-2">
              <button className="w-8 h-8 flex items-center justify-center rounded-[8px] bg-[#27272a] border border-[#3f3f46] text-foreground-muted">
                <MagnifyingGlass size={16} />
              </button>
              <button className="w-8 h-8 flex items-center justify-center rounded-[8px] bg-[#27272a] border border-[#3f3f46] text-foreground-muted">
                <ArrowsOut size={16} />
              </button>
              <button className="w-8 h-8 flex items-center justify-center rounded-[8px] bg-[#27272a] border border-[#3f3f46] text-foreground-muted">
                <Plus size={16} />
              </button>
              <button className="w-8 h-8 flex items-center justify-center rounded-[8px] bg-[#27272a] border border-[#3f3f46] text-foreground-muted">
                <Minus size={16} />
              </button>
              <button className="w-8 h-8 flex items-center justify-center rounded-[8px] bg-[#27272a] border border-[#3f3f46] text-foreground-muted">
                <NavigationArrow size={16} />
              </button>
            </div>
            {/* Map content area — placeholder text */}
            <div className="flex flex-1 items-center justify-center">
              <p className="text-sm text-foreground-muted">Interactive map — Phase 3</p>
            </div>
            {/* Bottom-right feedback note */}
            <div className="absolute bottom-3 right-3 rounded-[8px] bg-[#27272a] border border-[#3f3f46] px-3 py-2">
              <p className="text-xs text-foreground-muted">Map feedback</p>
            </div>
          </div>
        </div>
      )
    }
    ```

    Note on Diamond icon: If `Diamond` is not available in @phosphor-icons/react/ssr, use `Compass` instead (already verified as present). Check node_modules/@phosphor-icons/react/dist/ to confirm Diamond exists before using it; fall back to `Compass` if not.

    **RightColumn.tsx** — Right panel (fixed 544px width). Three stacked sections separated by border-t: Blockage Predictions, Condition Changes, Today's Rainfall. Accepts DashboardMetrics as props.

    ```tsx
    // src/components/dashboard/RightColumn.tsx
    // Server Component — no 'use client'
    import {
      Prohibit,
      XCircle,
      Warning,
      CheckCircle,
      CellSignalX,
      SecurityCamera,
      TrendDown,
      FileDashed,
      TrendUp,
      Minus,
      Umbrella,
    } from '@phosphor-icons/react/ssr'
    import type { DashboardMetrics } from '@/lib/data/dashboard'
    import { MetricStateCard } from './MetricStateCard'
    import { SectionTitle } from './SectionTitle'
    import { cn } from '@/lib/utils/cn'

    interface RightColumnProps {
      metrics: DashboardMetrics
      className?: string
    }

    export function RightColumn({ metrics, className }: RightColumnProps) {
      return (
        <div
          className={cn(
            'flex flex-col w-[544px] shrink-0 rounded-[12px] border border-[#3f3f46] bg-[#27272a] overflow-hidden min-h-0',
            className
          )}
        >
          {/* 1. Blockage Predictions */}
          <div className="flex flex-col gap-[10px] p-5">
            <SectionTitle
              icon={<Prohibit size={24} className="text-foreground-muted" />}
              title="Blockage Predictions"
              linkText="View predictions"
            />
            <div className="grid grid-cols-2 gap-[10px]">
              <MetricStateCard
                value={String(metrics.reactiveCount)}
                label="Requires reactive attention"
                labelColor="text-[#ef4444]"
                icon={<XCircle size={24} className="text-[#ef4444]" />}
              />
              <MetricStateCard
                value={String(metrics.proactiveCount)}
                label="Needs proactive attention"
                labelColor="text-[#facc15]"
                icon={<Warning size={24} className="text-[#facc15]" />}
              />
              <MetricStateCard
                value={String(metrics.allClearCount)}
                label="All clear"
                labelColor="text-[#22c55e]"
                icon={<CheckCircle size={24} className="text-[#22c55e]" />}
              />
              <MetricStateCard
                value={String(metrics.noSignalCount)}
                label="Sensors not responding"
                labelColor="text-[#a1a1aa]"
                icon={<CellSignalX size={24} className="text-[#a1a1aa]" />}
              />
            </div>
            <p className="text-sm text-[#71717a]">
              {metrics.iotMonitoredCount} assets monitored by IoT devices
            </p>
          </div>

          {/* 2. Condition Changes */}
          <div className="flex flex-col gap-[10px] p-5 border-t border-[#3f3f46]">
            <SectionTitle
              icon={<SecurityCamera size={24} className="text-foreground-muted" />}
              title="Condition Changes"
              linkText="View changes"
            />
            <div className="grid grid-cols-2 gap-[10px]">
              <MetricStateCard
                value={String(metrics.worsenedCount)}
                label="Worsened since last inspection"
                labelColor="text-[#ef4444]"
                icon={<TrendDown size={24} className="text-[#ef4444]" />}
              />
              <MetricStateCard
                value={String(metrics.noRecordsCount)}
                label="Needs maintenance records"
                labelColor="text-[#a1a1aa]"
                icon={<FileDashed size={24} className="text-[#a1a1aa]" />}
              />
              <MetricStateCard
                value={String(metrics.improvedCount)}
                label="Improved since last inspection"
                labelColor="text-[#22c55e]"
                icon={<TrendUp size={24} className="text-[#22c55e]" />}
              />
              <MetricStateCard
                value={String(metrics.unchangedCount)}
                label="No significant change"
                labelColor="text-[#3b82f6]"
                icon={<Minus size={24} className="text-[#3b82f6]" />}
              />
            </div>
            <p className="text-sm text-[#71717a]">
              Showing data from inspections completed this year to date
            </p>
          </div>

          {/* 3. Today's Rainfall */}
          <div className="flex flex-col gap-[10px] p-5 border-t border-[#3f3f46]">
            <SectionTitle
              icon={<Umbrella size={24} className="text-foreground-muted" />}
              title="Today's Rainfall"
            />
            <div className="flex gap-[10px]">
              <MetricStateCard
                value="0.51"
                label="Total rain (in)"
                labelColor="text-[#3b82f6]"
                icon={<span />}
                className="flex-1"
              />
              <MetricStateCard
                value="0.05"
                label="Intensity avg. (in/hr)"
                labelColor="text-[#3b82f6]"
                icon={<span />}
                className="flex-1"
              />
              <MetricStateCard
                value="0.17"
                label="Year avg. (in/hr)"
                labelColor="text-[#3b82f6]"
                icon={<span />}
                className="flex-1"
              />
            </div>
            <div className="flex flex-col gap-1">
              <p className="text-sm text-[#71717a]">Feb 19, 2026 — data current as of 08:00</p>
              <p className="text-[10px] font-medium italic text-[#71717a]">
                Source: NOAA National Weather Service
              </p>
            </div>
          </div>
        </div>
      )
    }
    ```

    Notes:
    - The rainfall MetricStateCard cells have no meaningful icon — pass `icon={<span />}` as a no-op placeholder. Alternatively, adjust MetricStateCard to make icon optional if that's cleaner.
    - If `SecurityCamera` or `FileDashed` are not available in @phosphor-icons/react/ssr, check node_modules and use the closest available alternative (e.g., `Camera` for SecurityCamera, `FileX` for FileDashed).
    - Verify all icon names against the actual files in node_modules/@phosphor-icons/react/dist/ssr/ before writing — icon names are PascalCase matching the Phosphor icon library.
  </action>
  <verify>
    Run: cd /Users/philipdavies/perispek && npx tsc --noEmit 2>&1 | grep -E "MapPlaceholder|RightColumn|error TS"
    Confirm: No TypeScript errors for these files.
  </verify>
  <done>MapPlaceholder.tsx and RightColumn.tsx exist; both compile without TypeScript errors; RightColumn accepts DashboardMetrics props and renders all three sections.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the dashboard page</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
    Replace the stub content in src/app/dashboard/page.tsx with the full dashboard layout. This is a Server Component — no 'use client' directive.

    Layout structure:
    - Outer div: `flex flex-col h-full overflow-hidden` — fills the main element, prevents scrolling
    - Top bar (h=80px, shrink-0): page title + search + share
    - Functions bar (h=80px, shrink-0): search + settings + date filter
    - Content area (flex-1 min-h-0): padding px-5 pb-5, gap-[10px], flex-col
      - Metrics row (h-[170px] shrink-0): 4 MetricLargeCard in a flex row
      - Below-metrics (flex-1 min-h-0 flex): MapPlaceholder + RightColumn

    Full implementation:

    ```tsx
    // src/app/dashboard/page.tsx
    // Server Component — no 'use client'
    import {
      Compass,
      Drop,
      WarningDiamond,
      Wrench,
      Exam,
      MagnifyingGlass,
    } from '@phosphor-icons/react/ssr'
    import { computeDashboardMetrics } from '@/lib/data/dashboard'
    import { MetricLargeCard } from '@/components/dashboard/MetricLargeCard'
    import { MapPlaceholder } from '@/components/dashboard/MapPlaceholder'
    import { RightColumn } from '@/components/dashboard/RightColumn'

    export default function DashboardPage() {
      const metrics = computeDashboardMetrics()

      return (
        <div className="flex flex-col h-full overflow-hidden">

          {/* Top bar — 80px */}
          <div className="flex items-center justify-between h-[80px] shrink-0 px-5 py-5 border-b border-[#3f3f46]">
            {/* Left: icon + title */}
            <div className="flex items-center gap-2">
              <Compass size={24} className="text-foreground-muted" />
              <span className="text-[20px] font-medium text-foreground leading-6">
                System Overview Dashboard
              </span>
            </div>
            {/* Right: search + share */}
            <div className="flex items-center gap-[10px]">
              <div className="relative flex items-center w-[334px]">
                <MagnifyingGlass
                  size={16}
                  className="absolute left-3 text-foreground-muted"
                />
                <input
                  type="text"
                  placeholder="Search..."
                  className="w-full h-9 pl-9 pr-3 rounded-[8px] bg-transparent border border-[#3f3f46] text-sm text-foreground placeholder:text-foreground-muted outline-none"
                  readOnly
                />
              </div>
              <button className="h-9 px-4 rounded-[8px] border border-[#3f3f46] text-sm font-medium text-foreground">
                Share
              </button>
            </div>
          </div>

          {/* Functions bar — 80px */}
          <div className="flex items-center h-[80px] shrink-0 px-5 py-5 gap-[10px] border-b border-[#3f3f46]">
            <div className="relative flex items-center w-[334px]">
              <MagnifyingGlass
                size={16}
                className="absolute left-3 text-foreground-muted"
              />
              <input
                type="text"
                placeholder="Search assets, alerts, work orders..."
                className="w-full h-9 pl-9 pr-3 rounded-[8px] bg-transparent border border-[#3f3f46] text-sm text-foreground placeholder:text-foreground-muted outline-none"
                readOnly
              />
            </div>
            <button className="h-9 px-4 rounded-[8px] border border-[#3f3f46] text-sm font-medium text-foreground shrink-0">
              Dashboard settings
            </button>
            <button className="h-9 px-4 rounded-[8px] border border-[#3f3f46] text-sm font-medium text-foreground shrink-0">
              Year to date
            </button>
            <span className="ml-auto text-sm text-foreground-muted shrink-0">
              Displaying data for Jan 1, 2026 – Feb 12, 2026
            </span>
          </div>

          {/* Main content area — flex-1, no overflow */}
          <div className="flex flex-col flex-1 min-h-0 px-5 pb-5 pt-[10px] gap-[10px]">

            {/* Primary metrics row — 4 cards, fixed height 170px */}
            <div className="flex gap-[10px] h-[170px] shrink-0">
              <MetricLargeCard
                icon={<Drop size={24} className="text-foreground-muted" />}
                label="Risk of overflow"
                value={`${metrics.overflowAssetCount} assets`}
                description={`${metrics.iotMonitoredCount} assets monitored`}
                linkText="View assets"
              />
              <MetricLargeCard
                icon={<WarningDiamond size={24} className="text-foreground-muted" />}
                label="Avg. risk score"
                value={String(metrics.avgRiskScore)}
                description={`${metrics.highAndCriticalCount} assets at high risk`}
                linkText="View high risk scores"
              />
              <MetricLargeCard
                icon={<Wrench size={24} className="text-foreground-muted" />}
                label="Work orders"
                value={`${metrics.openWorkOrderCount} open`}
                description={`${metrics.highPriorityWorkOrderCount} are high priority`}
                linkText="View work orders"
              />
              <MetricLargeCard
                icon={<Exam size={24} className="text-foreground-muted" />}
                label="Inspections this month"
                value={`${metrics.monthlyLinearFeet.toLocaleString()} ft`}
                description={`${metrics.monthlyPct}% of consent decree target`}
                linkText="View details"
              />
            </div>

            {/* Below-metrics: map (left) + right column */}
            <div className="flex flex-1 gap-[10px] min-h-0">
              <MapPlaceholder className="flex-1 min-h-0" />
              <RightColumn metrics={metrics} />
            </div>

          </div>
        </div>
      )
    }
    ```

    Notes on icon availability:
    - `Exam` (clipboard with check) is the Figma spec icon for inspections. Verify it exists in @phosphor-icons/react/ssr. If not, use `ClipboardText` or `Note` as fallback.
    - `Compass` for the page title — verified present in Phase 1.
    - All other icons (Drop, WarningDiamond, Wrench, MagnifyingGlass) were verified in RESEARCH.md.

    CRITICAL layout rules from RESEARCH.md:
    - The root layout already has: `<main className="flex-1 overflow-auto">`. The dashboard page renders inside this main. Using `h-full overflow-hidden` on the outer div tells the page to fill and contain its content.
    - Use `shrink-0` on both bars (80px each) and `flex-1 min-h-0` on the content area below.
    - The below-metrics area uses `flex-1 min-h-0` to prevent overflow. RightColumn has a fixed w-[544px] shrink-0. MapPlaceholder takes remaining space with `flex-1 min-h-0`.
  </action>
  <verify>
    Run: cd /Users/philipdavies/perispek && npx tsc --noEmit 2>&1 | head -20
    Run: cd /Users/philipdavies/perispek && npx next build 2>&1 | tail -15
    Confirm: TypeScript clean; build succeeds.
  </verify>
  <done>src/app/dashboard/page.tsx renders the full layout with computed seed data; build passes; no TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify dashboard renders correctly in browser</name>
  <action>Human verification of the complete dashboard layout in browser.</action>
  <verify>User confirms dashboard renders correctly at http://localhost:3000/dashboard.</verify>
  <done>User types "approved" to confirm the dashboard looks correct.</done>
  <what-built>
    Complete dashboard at /dashboard:
    - Top bar (80px): "System Overview Dashboard" title with compass icon + search + Share button
    - Functions bar (80px): search + "Dashboard settings" + "Year to date" buttons + date range text
    - 4 metric cards (170px): Risk of overflow / Avg. risk score / Work orders / Inspections this month — all populated from seed data
    - Map placeholder (left): "Risk Score" section title + dark placeholder card with toolbar chrome
    - Right column (right): Blockage Predictions 2×2 grid + Condition Changes 2×2 grid + Today's Rainfall 3-cell row

    To start the dev server: cd /Users/philipdavies/perispek && npm run dev
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000/dashboard in the browser.
    2. Confirm no scroll bar appears — the dashboard fills the viewport exactly.
    3. Confirm the 4 metric cards appear in a horizontal row with correct icons and values:
       - "Risk of overflow" showing a small number (expect ~2) + "N assets monitored"
       - "Avg. risk score" showing a decimal like 3.4 + "N assets at high risk"
       - "Work orders" showing "N open" + "N are high priority"
       - "Inspections this month" showing "N,NNN ft" + "N% of consent decree target"
    4. Confirm the map placeholder appears on the left (dark area with "Interactive map — Phase 3" text) and the right column is visible with all 3 sections.
    5. Confirm the Blockage Predictions grid shows 4 cells with colored labels (red, yellow, green, grey).
    6. Confirm the Condition Changes grid shows 4 cells with correct colored labels.
    7. Confirm Today's Rainfall shows 3 cells with values 0.51, 0.05, 0.17 in blue.
    8. Check browser console — no React errors or hydration warnings.
  </how-to-verify>
  <resume-signal>Type "approved" if the dashboard looks correct, or describe any visual issues for fixing.</resume-signal>
</task>

</tasks>

<verification>
1. src/app/dashboard/page.tsx is fully implemented (not a stub).
2. MapPlaceholder.tsx and RightColumn.tsx exist and compile.
3. npx tsc --noEmit shows 0 errors.
4. npx next build succeeds.
5. Human verified: dashboard renders at /dashboard with correct layout, no scroll, real seed data values.
</verification>

<success_criteria>
- /dashboard renders a no-scroll, full-viewport dashboard layout.
- All metric card values are computed from seed data (no hardcoded values in page.tsx).
- Right column shows all 3 sections: Blockage Predictions, Condition Changes, Today's Rainfall.
- No TypeScript errors; build passes.
- Human approves the visual output.
</success_criteria>

<output>
After completion, create `/Users/philipdavies/.planning/phases/02-dashboard/02-03-SUMMARY.md` following the summary template.
</output>
